apiVersion: "spec.keptn.sh/0.2.2"
kind: "Shipyard"
metadata:
  name: "e2e-deployment-shipyard"
spec:
  stages:
    - name: "e2e"
      sequences:
        - name: "jes"
          tasks:
            - name: "deployment"
