apiVersion: spec.keptn.sh/0.2.2
kind: "Shipyard"
metadata:
  name: "shipyard-locust"
spec:
  stages:
    - name: "qa"
      sequences:
        - name: "test"
          tasks:
            - name: "test"
