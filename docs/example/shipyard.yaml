apiVersion: "spec.keptn.sh/0.2.2"
kind: "Shipyard"
metadata:
  name: "shipyard-jes-sample"
spec:
  stages:
    - name: "production"
      sequences:
        - name: "example-seq"
          tasks:
            - name: "remote-task"